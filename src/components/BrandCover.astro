<section class="brand-cover" aria-labelledby="brand-cover-title">
  <h1 id="brand-cover-title" class="brand-wordmark">homoludens<span>.</span></h1>
  <p>development · retrospective · books</p>
</section>

<style>
  .brand-cover {
    display: grid;
    min-height: clamp(156px, 19vw, 220px);
    padding: 28px 24px;
    border-bottom: 1px solid var(--line);
    background: var(--surface-soft);
    align-content: center;
    justify-items: center;
  }

  .brand-wordmark {
    margin: 0;
    font-size: clamp(2.8rem, 7vw, 5rem);
    font-weight: 720;
    line-height: 0.95;
    letter-spacing: -0.055em;
  }

  .brand-wordmark span {
    color: var(--accent);
  }

  .brand-cover p {
    margin: 16px 0 0;
    color: var(--text-muted);
    font-size: 0.75rem;
    font-weight: 650;
    letter-spacing: 0.11em;
    text-transform: uppercase;
  }

  @media (max-width: 620px) {
    .brand-cover {
      min-height: 142px;
    }

    .brand-cover p {
      margin-top: 12px;
      font-size: 0.68rem;
    }
  }
</style>
