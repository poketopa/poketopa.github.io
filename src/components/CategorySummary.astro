---
import type { Category } from '../consts';

interface Props {
  category: Category | 'all';
  count: number;
}

const { category, count } = Astro.props;
const suffix = {
  all: '개의 기록이 쌓였습니다.',
  Development: '개의 경험을 기록했습니다.',
  Retrospective: '번을 돌아봤습니다.',
  Books: '권을 읽었습니다.',
}[category];
---

<p class="category-summary"><strong>{count}</strong>{suffix}</p>
